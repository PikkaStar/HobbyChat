<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2 class="text-center"><%= @user.name %>さんがいいねした投稿一覧</h2>

      <div class="d-flex flex-wrap">
       <%= render 'user/posts/index', posts: @posts %>
      </div>
      <div class="d-flex justify-content-center">
      <%= paginate @posts %>
      </div>
    </div>
  </div>
</div>